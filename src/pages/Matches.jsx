import React from 'react';
import '../estilos/Matches.css'

const Matches = () => {
  // Obtener los matches del usuario desde la base de datos k veremos luego

  return (
    <div className="container">
      <h1>Matches</h1>
      {/* Contenido de los matches realizados */}
      {/* Lista de perfiles con los que ha habido match */}
    </div>
  );
};

export default Matches;
